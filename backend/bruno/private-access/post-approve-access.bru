meta {
  name: POST approve private access
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/private-access/requests/{{privateRequestId}}/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{targetToken}}
}
